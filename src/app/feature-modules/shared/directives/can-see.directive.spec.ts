import { CanSeeDirective } from './can-see.directive';

describe('CanSeeDirective', () => {
  it('should create an instance', () => {
    const directive = new CanSeeDirective();
    expect(directive).toBeTruthy();
  });
});
